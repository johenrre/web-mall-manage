# DIY 商城管理后台功能需求文档

## 文档说明

| 项目 | 内容 |
| --- | --- |
| 文档类型 | 基于需求及 Vue 3 重构实现整理的功能需求文档 |
| 分析对象 | `web-mall-manage/src`（`admin.html` 为兼容跳转入口） |
| 辅助分析 | `web-mall-manage/serve-admin.js` |
| 文档版本 | V1.0 |
| 整理日期 | 2026-07-20 |

> 管理后台目录目前没有 `index.html`，实际入口文件是 `admin.html`。本文描述的是当前代码已经体现的业务功能，同时将代码中的限制、风险和待完善项单独列出，不将其误写成已完成能力。

## 1. 产品概述

### 1.1 产品定位

管理后台用于运营 DIY 串珠商城，覆盖订单履约、退款售后、盘珠资料、用户、设计作品、设计师申请、提现、现金卡券、经营统计、结算定制选项以及商城全局配置。

### 1.2 使用角色

当前后台只定义一个后台操作角色：管理员。

- 管理员登录后可以访问全部后台菜单和操作。
- 普通用户不能凭普通用户身份进入后台。
- 用户资料中可把用户角色修改为 `admin` 或 `user`，但后台尚未实现更细粒度的菜单、数据和操作权限。

### 1.3 运行及接口依赖

- 管理后台开发服务默认监听 `3001` 端口。
- Vite 开发服务及 `serve-admin.js` 将 `/api/*`、`/uploads/*` 请求代理到默认监听 `3000` 端口的后端服务。
- 页面默认使用同源 API，也可通过环境变量 `VITE_API_BASE` 指定后端地址。
- 管理员令牌仅保存在当前标签会话的 `sessionStorage` 中，同时兼容后端 HttpOnly Cookie；请求通过 `Authorization: Bearer <token>` 和 `X-Admin-Token` 发送。
- 新版接口统一使用无 `.php` 扩展名的 `/api/*` 地址，后端仅为历史客户端保留旧地址兼容映射。

## 2. 信息架构

后台包含以下一级菜单：

| 序号 | 菜单 | 主要用途 |
| --- | --- | --- |
| 1 | 订单管理 | 查询订单、查看详情、修改状态、发货、删除和清理超时订单 |
| 2 | 售后管理 | 审核退款售后、展示退货地址、与用户沟通 |
| 3 | 盘珠管理 | 维护盘珠、主分类和子分类 |
| 4 | 数据统计 | 查看销售额、订单状态和热门盘珠 |
| 5 | 提现管理 | 审核并处理用户提现申请 |
| 6 | 现金卡券 | 创建现金卡券并查看领取情况 |
| 7 | 用户管理 | 查询、编辑、充值和删除用户 |
| 8 | 设计管理 | 审核作品、设置作品类型和维护实拍图 |
| 9 | 设计师管理 | 审核设计师申请及设计师作品 |
| 10 | 定制选项 | 配置制作、包装、绳色等结算加价选项 |
| 11 | 系统设置 | 配置推广、商城展示、登录、支付和内容资料 |

默认进入“订单管理”。订单和售后菜单可显示待处理数量角标。

## 3. 全局功能需求

### 3.1 身份认证

#### AUTH-01 管理员登录

- 管理员必须输入账号和密码。
- 系统调用管理员登录接口校验身份。
- 登录成功后保存管理员令牌，关闭登录窗口并展示后台。
- 登录失败时展示后端返回的错误信息。
- 登录请求处理中应禁用重复提交。

#### AUTH-02 登录态校验

- 页面初始化时，如本地存在令牌，系统应调用令牌校验接口。
- 令牌有效时直接进入后台；令牌无效时清除令牌并展示登录窗口。
- 任一管理接口返回 401 或 403 时，应清除登录态并要求重新登录。

#### AUTH-03 退出登录

- 管理员点击退出时应进行二次确认。
- 确认后清除本地令牌并返回登录状态。

#### AUTH-04 修改密码

- 管理员可依次输入原密码、新密码和确认密码。
- 两次新密码必须一致。
- 修改成功后应退出当前登录，要求使用新密码重新登录。

### 3.2 通知和刷新

- 系统每 20 秒获取一次后台通知摘要。
- “订单管理”角标展示“已付款、待发货”的订单数。
- “售后管理”角标展示存在未读售后消息的订单数。
- 售后对话打开时，每 8 秒刷新一次消息。
- 各业务列表应具有加载中、空数据和失败提示。

### 3.3 通用交互

- 删除、拒绝、清理等不可逆或高影响操作必须二次确认。
- 成功或失败结果应明确反馈给管理员。
- 日期时间统一显示为本地可读格式，金额统一以人民币元展示。
- 图片应支持预览；上传图片大小上限为 5 MB。

## 4. 详细功能需求

### 4.1 订单管理

#### ORD-01 订单查询与筛选

- 系统一次最多请求 1000 条订单，在前端按每页 20 条分页展示。
- 管理员可按订单 ID、订单编号、用户名或用户昵称搜索。
- 管理员可按以下状态筛选：全部、待付款、已付款、已发货、已完成、已取消、退款/售后。
- 状态标签旁应显示对应订单数量。

#### ORD-02 订单列表

列表至少展示：订单号、用户、收件人、手机号、设计名称、珠子数量、订单金额、订单状态、创建时间和操作入口。

#### ORD-03 订单详情

管理员可查看：

- 订单编号、用户、收件人、电话、地址、总金额、状态和创建时间。
- 支付时间、第三方交易号及支付渠道说明。
- 快递公司、快递单号和发货时间。
- 设计名称、饰品类型（手链/项链）、珠子数量、制作方式、包装方式、配送方式、绳色和绳色图片。
- 按圆形布局展示串珠设计预览。
- 点击单颗珠子查看名称、位置、尺寸、颜色、色系、珠子 ID 和分类。
- 汇总相同珠子的使用数量。

#### ORD-04 修改订单状态

管理员可将订单状态修改为：待付款、已付款、已发货、已完成或已取消。

#### ORD-05 订单发货

- 管理员填写快递公司和快递单号后提交发货。
- 快递公司和快递单号均为必填项。
- 发货成功后应刷新订单数据。

#### ORD-06 删除订单

- 管理员可删除单个订单。
- 删除前必须明确提示并二次确认。

#### ORD-07 清理超时未支付订单

- 管理员可一键清理超过 30 分钟仍未支付的订单。
- 清理前必须二次确认。
- 操作完成后展示实际删除数量并刷新列表。

### 4.2 售后管理

售后管理复用订单管理界面，并默认筛选退款/售后订单。

#### AFS-01 售后信息

管理员应能查看售后状态、申请原因、审核备注和系统配置的退货地址。

#### AFS-02 售后审核

- 管理员可执行“通过”“拒绝”和“处理完成”。
- 操作时可填写审核备注；拒绝原因应由业务侧强制要求填写。
- 操作成功后刷新订单和售后状态。

售后状态包括：处理中、已通过待退回、已拒绝、已处理完成、已撤回。

#### AFS-03 售后沟通

- 管理员可查看用户与客服的消息记录及状态变更记录。
- 消息应区分用户和客服发送方。
- 历史消息中的图片可点击查看大图。
- 管理员可发送文字消息，按 Enter 可快捷发送。
- 当前管理员发送端仅提交文字，尚未提供售后图片上传能力。

### 4.3 盘珠管理

#### BEAD-01 查询和筛选

- 管理员可按简体中文名、英文名和繁体中文名搜索。
- 管理员可按主分类筛选。
- 列表展示名称、英文名、分类、排序值、色系、材质类型、尺寸、价格、图片和操作。

#### BEAD-02 新增和编辑盘珠

盘珠字段包括：

- 简体中文名、英文名、繁体中文名。
- 主分类、子分类、分类排序值。
- 色系、材质类型、尺寸、价格。
- 图片地址和描述。

名称、材质类型、尺寸和价格为必填项；分类为必选项。非“配饰”分类需要选择色系，“配饰”分类需要选择子分类。

材质类型目前包括：木质、石质、玻璃、金属。

#### BEAD-03 盘珠图片

- 管理员可直接输入图片 URL，页面应显示预览。
- 管理员可上传 JPG、PNG、GIF 或 WebP 图片。
- 图片不得超过 5 MB。
- 上传结果可来自七牛云或本地存储。

#### BEAD-04 删除盘珠

管理员可删除盘珠，删除前必须二次确认。

#### BEAD-05 主分类管理

- 默认主分类包括：水晶、沉香、菩提、文玩、配饰。
- 管理员可新增、重命名、删除主分类，并可左右调整展示顺序。
- 主分类名称不得重复。
- 重命名时应同步修改已使用该分类的盘珠数据。
- 删除已被盘珠使用的分类时必须给出风险提示。

#### BEAD-06 子分类/色系管理

- 每个主分类拥有独立的子分类配置。
- “配饰”使用业务子分类，如花托、跑环、魔盒、装饰、隔珠、吊坠。
- 其他分类主要使用色系，如白、绿、黄、棕、红、蓝、紫、黑；实际可选范围随主分类变化。
- 管理员可新增、重命名、删除和调整子分类顺序。
- 重命名时应同步更新已使用该值的盘珠数据。

### 4.4 数据统计

#### STAT-01 经营概览

展示总销售额、订单总数、已支付订单数和支付转化率。

#### STAT-02 订单状态统计

按订单状态展示订单数量。

#### STAT-03 热门盘珠

展示盘珠图片、名称和被使用次数。

#### STAT-04 数据刷新

页面进入时自动加载，管理员也可手动刷新统计数据。

### 4.5 提现管理

#### WDR-01 提现列表

列表展示提现 ID、用户、金额、支付宝账号、收款姓名、状态、申请时间和操作。

#### WDR-02 提现处理

- 对待处理申请，管理员可标记为“已打款”或“拒绝”。
- 处理时可填写备注，拒绝时应填写拒绝原因。
- 已处理记录展示管理员备注。

### 4.6 现金卡券

#### CPN-01 创建现金卡券

- 管理员填写卡券标题、金额和发行数量。
- 标题和金额必填，金额按两位小数处理。
- 发行数量填写 0 表示不限量。

#### CPN-02 卡券列表

展示卡券 ID、名称、金额、已领取数量、总发行量、状态和创建时间。

当前界面只支持创建和查看，未提供编辑、删除、启用或停用操作。

### 4.7 用户管理

#### USR-01 用户查询

- 支持按用户名或昵称搜索。
- 后端分页，每页 20 条。
- 列表展示用户 ID、用户名、昵称、角色、余额、订单数量、注册时间和操作。

#### USR-02 编辑用户

- 管理员可编辑用户昵称和角色。
- 用户名只读，不允许在后台修改。
- 角色可设置为管理员或普通用户。

#### USR-03 用户充值

- 管理员选择用户后输入充值金额和原因。
- 充值金额最小为 0.01 元。
- 默认充值原因为“管理员充值”。
- 成功后展示充值前和充值后的余额。

#### USR-04 删除用户

管理员可删除用户，删除前必须提示该操作不可恢复并二次确认。

### 4.8 设计管理

#### DSG-01 作品查询

- 支持按作品名称、用户和作品 ID 搜索。
- 支持按作品类型筛选：全部、设计师作品、优秀客订。
- 支持按状态筛选：已展示、待处理、已下架、未发布和全部。
- 默认查看已展示作品，支持手动刷新。

#### DSG-02 作品列表及预览

列表展示作品 ID、圆形串珠预览、作品名称、用户、价格、珠子数量、实拍图、状态、作品类型、提交时间和操作。

管理员可打开大图弹窗预览串珠设计。

#### DSG-03 作品审核和分类

- 管理员可将作品审核为“优秀客订”。
- 管理员可将作品审核为“设计师作品”。
- 管理员可拒绝或下架作品，并填写原因。

作品状态包括：待处理、已展示、已下架、未发布。

#### DSG-04 实拍图管理

- 管理员可上传作品实拍图并保存 URL 列表。
- 单张图片不得超过 5 MB。
- 管理员可删除单张实拍图。

### 4.9 设计师管理

设计师管理包含“申请审核”和“作品审核”两个子页。

#### CRT-01 设计师申请审核

- 列表展示申请 ID、用户、真实姓名、联系方式、个人介绍、状态和申请时间。
- 管理员可通过或拒绝待审核申请。
- 拒绝申请时必须填写原因。

#### CRT-02 设计师作品审核

- 列表展示作品 ID、封面、用户、标题、描述、状态和提交时间。
- 管理员可通过或拒绝待审核作品。
- 通过时可指定是否设为精选作品。
- 拒绝时必须填写原因。

审核状态包括：待审核、已通过、已拒绝。

### 4.10 定制选项

该模块控制商城定制详情/结算页可选择的附加服务和加价。

#### OPT-01 默认配置

系统内置以下默认分组：

- 制作方式：自己制作（免费）、成品制作（默认，另加 9.9 元）。
- 包装方式：普通包装（免费）、礼盒包装（默认，另加 10 元）。
- 绳线颜色：红、黑、浅蓝、白、紫、橙、粉、绿，默认各加 2 元并带图片路径。

#### OPT-02 分组管理

- 管理员可恢复默认配置。
- 管理员可新增和删除分组。
- 分组字段包括分组标题、分组编码和默认选项编码。

#### OPT-03 选项管理

- 管理员可新增和删除分组内选项。
- 选项字段包括名称、编码、说明、可选图片 URL、加价金额和是否免费。
- 勾选免费后，加价金额自动归零。
- 删除当前默认选项后，应自动选中该分组剩余的第一个选项。

#### OPT-04 保存和容错

- 保存时将配置标准化后写入系统设置。
- 无效或空的分组、选项应被过滤或补全，避免商城端读取失败。

### 4.11 系统设置

#### SET-01 推广设置

- 配置一级推广分佣比例和二级推广分佣比例。
- 页面输入范围为 0%～100%，支持两位小数；保存到后端时转换为 0～1 的小数。
- 新支付成功的订单按最新比例计算。

#### SET-02 盘珠分类顺序

- 使用逗号分隔的分类名称配置展示顺序。
- 该设置只影响展示顺序，不修改盘珠本身的数据。

#### SET-03 小程序微信登录

- 可启用或停用小程序微信登录。
- 配置小程序 AppID 和 AppSecret。
- 小程序登录接口优先使用此处配置。

#### SET-04 小程序和站点展示

可配置：

- 小程序名称。
- 首页底部图片、盘子中间 Logo、站点标题 Logo。
- 购买须知、积分规则说明。
- 首页轮播图 JSON。
- 联系客服二维码和微信号。
- 首页盘珠教程步骤。
- 关于我们中英文资料。
- 品牌理念中英文资料。
- 四步使用指南中英文资料。
- 购买须知条目中英文资料。
- 手腕测量步骤中英文资料。
- 珠子尺寸说明中英文资料。
- 网站 Favicon。

#### SET-05 首页轮播图

- 支持上传图片后自动追加一条轮播配置。
- 支持直接编辑 JSON。
- JSON 顶层必须是数组，否则应阻止预览或追加图片并提示错误。
- 单条轮播支持 `image`、`tag`、`title`、`desc`、`targetPath`、`titleFontSize`、`descFontSize`。
- 提供卡片式预览，并支持删除单张轮播图。
- 配置留空时商城使用内置默认轮播。

#### SET-06 内容资料维护

- 教程、品牌理念、指南、测量步骤和尺寸说明支持新增、编辑和删除条目。
- 内容资料支持中英文成对维护。
- 需要图片的内容支持输入 URL 或上传图片，并提供预览。

#### SET-07 微信支付

- 可启用或停用小程序微信支付。
- 配置支付 AppID、商户号 MchID、API v3 Key、商户证书序列号、支付回调地址和商户 API 私钥。
- 支付回调地址必须可被公网访问。

#### SET-08 Google 登录

- 可启用或停用 Google 登录。
- 配置 Client ID、Client Secret 和 Redirect URI。

#### SET-09 Creem 支付

- 可启用或停用 Creem 支付。
- 配置 API Key、Product ID、API URL、支付成功返回地址、取消支付返回地址和 Webhook Secret。
- Webhook 路径为 `/api/pay/notify_creem`。

#### SET-10 退货地址

管理员可配置售后审核通过后向用户展示的退货地址及注意事项。

#### SET-11 设置图片上传

- 图片不得超过 5 MB。
- 上传请求携带管理员认证信息。
- 上传成功后只更新页面中的待保存值；管理员仍需点击“保存设置”才正式生效。

## 5. 业务状态和规则

### 5.1 订单状态

| 状态值 | 中文含义 | 后台动作 |
| --- | --- | --- |
| `pending` | 待付款 | 可手动设置、可被超时清理 |
| `paid` | 已付款 | 进入待发货提醒 |
| `shipped` | 已发货 | 保存物流后进入该状态 |
| `completed` | 已完成 | 可手动设置 |
| `cancelled` | 已取消 | 可手动设置 |
| `refund` | 退款/售后 | 进入售后处理流程 |

### 5.2 售后状态

| 状态值 | 中文含义 |
| --- | --- |
| `pending` | 处理中 |
| `approved` | 已通过待退回 |
| `rejected` | 已拒绝 |
| `done` | 已处理完成 |
| `cancelled` | 已撤回 |

### 5.3 审核状态

设计师申请/作品使用 `pending`、`approved`、`rejected`。商城设计另包含 `private`（未发布），并通过作品类型区分设计师作品和优秀客订。

## 6. 后端接口依赖

以下为 Vue 3 管理后台直接调用或明确依赖的接口。

| 业务 | 方法 | 接口 | 用途 |
| --- | --- | --- | --- |
| 认证 | POST | `/api/user/admin_login` | 管理员登录 |
| 认证 | GET | `/api/user/verify` | 校验登录令牌 |
| 认证 | POST | `/api/user/change_password` | 修改密码 |
| 用户 | GET | `/api/user/list` | 用户列表/搜索/分页 |
| 用户 | POST | `/api/user/update` | 更新昵称和角色 |
| 用户 | POST | `/api/user/recharge` | 管理员充值 |
| 用户 | POST | `/api/user/delete` | 删除用户 |
| 订单 | GET | `/api/order/list` | 订单列表 |
| 订单 | GET | `/api/order/detail` | 订单详情 |
| 订单 | POST | `/api/order/update_status` | 修改订单状态 |
| 订单 | POST | `/api/order/ship` | 保存物流并发货 |
| 订单 | POST | `/api/order/delete` | 删除订单 |
| 订单 | POST | `/api/order/cleanup` | 清理超时未支付订单 |
| 售后 | POST | `/api/order/refund_review` | 审核/完成售后 |
| 售后 | GET | `/api/order/refund_message_list` | 获取售后消息 |
| 售后 | POST | `/api/order/refund_message_send` | 发送售后消息 |
| 盘珠 | GET | `/api/bead/list` | 盘珠列表 |
| 盘珠 | POST | `/api/bead/create` | 新增盘珠 |
| 盘珠 | POST | `/api/bead/update` | 更新盘珠 |
| 盘珠 | POST | `/api/bead/delete` | 删除盘珠 |
| 盘珠 | POST | `/api/admin/bead_category_rename` | 同步重命名分类 |
| 统计 | GET | `/api/admin/stats` | 经营统计 |
| 提现 | GET | `/api/admin/withdraw_list` | 提现列表 |
| 提现 | POST | `/api/admin/withdraw_process` | 处理提现 |
| 卡券 | GET | `/api/admin/coupon_list` | 卡券列表 |
| 卡券 | POST | `/api/admin/coupon_create` | 创建卡券 |
| 设计师 | GET | `/api/admin/creator_applications_list` | 设计师申请列表 |
| 设计师 | POST | `/api/admin/creator_application_review` | 审核设计师申请 |
| 设计师 | GET | `/api/admin/creator_works_list` | 设计师作品列表 |
| 设计师 | POST | `/api/admin/creator_work_review` | 审核设计师作品 |
| 设计 | GET | `/api/admin/design_list` | 商城设计列表 |
| 设计 | POST | `/api/admin/design_review` | 审核、分类或下架设计 |
| 设计 | POST | `/api/admin/design_live_photos` | 保存作品实拍图 |
| 通知 | GET | `/api/admin/notifications_summary` | 待发货及售后消息数量 |
| 设置 | GET | `/api/admin/settings_get` | 读取系统设置 |
| 设置 | POST | `/api/admin/settings_update` | 保存系统设置 |
| 上传 | POST | `/api/upload/image` | 上传盘珠、作品及设置图片 |

## 7. 数据校验和异常处理

- 所有管理接口的成功响应以 `code === 200` 为成功判断依据。
- 接口错误优先展示后端返回的 `message`，网络错误应展示通用错误信息。
- 金额字段应转换为数值，不允许空值、非数字或超出业务范围的值提交。
- JSON 配置在保存和展示前必须校验格式及预期数据类型。
- 图片上传需校验文件存在、大小和返回 URL。
- 删除、拒绝、下架、提现处理、退款处理和批量清理应防止重复提交。
- 接口返回 401/403 时统一使当前登录失效。

## 8. 验收标准

### 8.1 认证验收

- 无有效管理员令牌时不能查看后台业务数据。
- 使用正确账号密码可登录；错误凭据能看到明确提示。
- 修改密码后旧登录态失效。

### 8.2 核心业务验收

- 订单可按关键字和状态查询，详情、串珠预览、发货、状态变更、删除和超时清理可完整闭环。
- 售后申请可通过、拒绝、完成处理，并可持续查看和发送售后消息。
- 盘珠及其分类可新增、编辑、删除和排序；分类重命名能同步已有盘珠。
- 用户可查询、编辑、充值和删除，充值前后余额正确。
- 提现、设计师申请、设计师作品和商城作品均可完成审核闭环。
- 结算定制选项保存后，商城端能读取正确的分组、默认项、免费项和加价金额。
- 系统设置保存后重新进入页面仍能正确回显，并能被相应商城功能读取。

### 8.3 兼容和异常验收

- 后端运行在 3000、后台运行在 3001 时，所有管理接口均可正常访问。
- 登录过期后，任何菜单操作都会回到登录状态，而不是停留在无权限或空白页面。
- 空列表、接口失败、非法 JSON、超大图片和缺失必填项都有明确反馈。

## 9. 当前实现限制与整改建议

以下内容是代码分析发现的现状，不代表已满足的需求。

1. **敏感信息泄露风险**：登录代码会把用户名、密码和令牌输出到浏览器控制台，应立即移除。
2. **令牌存储风险**：管理员令牌存放在 `localStorage`，一旦页面存在 XSS，令牌可能被读取；建议改为安全、HttpOnly、SameSite Cookie，或至少强化 CSP 和 XSS 防护。
3. **密钥展示风险**：Google Client Secret、Creem API Key/Webhook Secret、微信 API v3 Key 和商户私钥以普通文本字段回显，应脱敏、限制回传并设置单独的高权限操作和审计。
4. **盘珠上传认证不一致**：盘珠编辑区的图片上传请求未像系统设置和作品上传一样显式附带管理员令牌，后端严格校验时可能上传失败，需统一上传封装。
5. **订单列表扩展性**：当前一次拉取最多 1000 条订单再在浏览器分页，订单量增大后会造成遗漏和性能问题，应改为后端分页、后端搜索和后端状态统计。
6. **权限粒度不足**：当前只有管理员/普通用户，缺少客服、运营、财务、内容审核等岗位权限及数据范围控制。
7. **审计能力不足**：删除订单/用户、充值、提现、退款、下架、配置密钥等高风险操作未在页面体现审计日志和操作人记录。
8. **交互方式较原始**：大量操作依赖浏览器 `alert`、`prompt`、`confirm`，不利于字段校验、移动端适配和误操作防范，建议改为统一表单弹窗。
9. **售后发图不完整**：历史消息可看图片，但管理员发送接口固定提交空图片数组，后台没有上传售后凭证的入口。
10. **卡券功能不完整**：只能创建和查看，缺少编辑、停用、删除、有效期、适用范围和领取明细。
11. **统计展示不完整**：接口数据中包含设计数量，但当前统计界面未展示；也缺少日期范围、趋势图、退款金额和客单价等指标。
12. **作品发布规则需确认**：代码文案暗示用户投稿可能直接进入灵感展示，再由管理员分类或下架；若业务要求先审后发，应调整发布流程和默认状态。
13. **破坏性操作保护不足**：部分操作只有一次确认，尚无二次身份验证、幂等键、批量操作预览和可恢复机制。
14. **前端工程化不足**：后台全部逻辑集中在约 5300 行的单一 HTML 文件中，维护、测试和复用成本较高；建议拆为模块化 React 工程并补充自动化测试。

## 10. 后续需求扩展建议

如进入下一阶段开发，可优先补充：后台角色与权限、操作审计、后端分页、售后凭证上传、卡券生命周期、统计日期维度、敏感配置安全存储，以及订单/退款/提现操作的幂等和风控。
